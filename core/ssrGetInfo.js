import _ from "lodash";
// import { getHeaders, API } from "./config/api";

export const SSR_GetInfo = async () => {
  // const res = await fetch(`${API().core}auth/info`, {
  //   method: "GET",
  //   headers: await getHeaders(),
  // });

  // const responsive = await res.json();
  return {  };
};
